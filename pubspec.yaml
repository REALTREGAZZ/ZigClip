name: zigclip
description: Cyber Athlete Arena
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  video_player: ^2.8.2
  supabase_flutter: ^2.5.1
  flutter_animate: ^4.5.0
  just_audio: ^0.9.38

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
